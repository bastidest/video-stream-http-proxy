{
  "name": "video-stream-http-proxy",
  "version": "0.3.0",
  "description": "Reads multiple video streams (e.g. RTSP) and outputs low-latency DASH streams, accessible via a HTTP connection, viewable via a simple web interface",
  "main": "src/server/index.ts",
  "type": "module",
  "scripts": {
    "start": "TS_NODE_PROJECT=\"./tsconfig.json\" node --experimental-specifier-resolution=node --loader ./node_modules/ts-node/esm ./src/server/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@ltd/j-toml": "^1.30.0",
    "@types/express": "^4.17.13",
    "@types/ws": "^8.5.3",
    "ajv": "^8.11.0",
    "dashjs": "^4.4.0",
    "express": "^4.18.1",
    "onvif": "^0.6.5",
    "pug": "^3.0.2",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3",
    "ws": "^8.8.0"
  },
  "devDependencies": {
    "tslint": "^6.1.3"
  }
}
